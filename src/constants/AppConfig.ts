export const TIME_CACHE_DB = 2 * 60 * 1000;

export enum STATUS {
  DISABLE = 1,
  ENABLE = 0,
}

export enum DELETE_STATUS {
  YES = 1,
  NO = 0,
}

export enum VERIFY_STATUS {
  YES = 1,
  NO = 0,
}

export enum EMPLOYEE_TYPE {
  OWNER = 1,
  STAFF = 2,
}

export enum OTP_IS_USE {
  YES = 1,
  NO = 0,
}
